<?php

// $Id achievement_badge_manager.api.php

/*
 * Give multiple achievement to single user.
 */
function hook_openbadging_badge_manager_give_mau($entity, $context) {
    //give multiple achievement to single user.
}

/*
 * Take single achievement from multiple user.
 */

function hook_openbadging_badge_manager_take_amu($entity, $context) {
    //take single achievement from multiple user.
}

/*
 * Give single achievement to multiple user.
 */

function hook_openbadging_badge_manager_give_amu($entity, $context) {
    //give single achievement to multiple user.
}

/*
 * take multiple achievement from single user.
 */

function hook_openbadging_badge_manager_take_mau($entity, $context) {
    //take multiple achievement from single user.
}
